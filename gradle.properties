org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
kotlin.code.style=official
# Gemini API key is read from ~/.gradle/gradle.properties (GEMINI_API_KEY=...).
# Do not set it here - project-level gradle.properties takes precedence over the
# user-level one, so a value (even empty) here would override your local key.
